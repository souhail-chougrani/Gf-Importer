import { AppDragNdropDirective } from './app-drag-ndrop.directive';

describe('AppDragNdropDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDragNdropDirective();
    expect(directive).toBeTruthy();
  });
});
